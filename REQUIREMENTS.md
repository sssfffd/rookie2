# IO 로그 그래프 뷰어 — 요구사항

rookie2 저장소에서 새로 만들면서 받은 요구사항 전문입니다.
(dummy_rookie 의 C++ / Win32 + Direct2D 구현은 여기서 이어받지 않고 새로 썼습니다.)

---

## 0. 언어와 구조

- C# / python(ai 활용 모듈, 옵션 기능으로 존재, 추후 직접 구현 예정) / CMake 활용
- Visual Studio 2017
- Windows 데스크톱 프로그램. **Windows 7 / 10 / 11 셋 모두**에서 빌드되고 실행돼야 함.
- **외부 라이브러리는 보안 때문에 자제해야 함.** 안 쓰는 게 제일 좋고, 꼭 써야 하면
  뭘 왜 쓰는지랑 위험해 보이는 부분을 설명할 것.
- 설치 프로그램 없이 **파일만 복사해서 실행**할 수 있어야 함.
  런타임을 따로 깔아야 하면 그것도 미리 알릴 것.
- VS Code 도 쓰는데 확장은 안 깔 것. 확장 없이 빌드되는 진입점(build.bat 같은 것).
- **1000만 칸(채널 200 x 표본 5만) 정도의 엑셀 로그**를 열어서 부드럽게 그려야 함.
  메모리도 신경 쓸 것.
- 구조는 **파일 파싱/데이터 부분과 UI 부분이 나뉘어** 있을 것.

## 1. 입력 데이터

- 엑셀 파일(.xlsx)과 CSV 둘 다 읽을 수 있게.
- 로그는 시간축 한 줄 + 나머지가 각 시간별 데이터 값들. 각 줄의 첫 칸은 IO 이름.
- **행이 IO인 배치와 열이 IO인 배치 둘 다** 지원 (우리 로그는 열이 IO명).
  자동 판별 + 수동 지정 둘 다 가능하게.
- 엑셀 중간부터 log 데이터들이 존재함 (앞에 장비명/측정일시 같은 머리말).
  표가 시트 중간에서 시작해도 찾아낼 것.
- IO 이름은 엑셀에 있는 실제 이름을 그대로 가져올 것 (IO_1 같은 자동 이름 말고).
- 파일 크기 상한은 없게.
- 한 번 연 파일을 다시 열 수 있어야 함.
- 시간축 주의사항:
  - 시간 칸이 값 바뀔 때만 적혀 있고 중간이 비어 있어도 시간축으로 인식.
  - 같은 시각이 여러 표본에 반복되면 다음 시각까지 고르게 펴서 배치.
  - 시각이 전부 같으면 샘플 번호를 시간축으로.
  - 0이 몇 분 동안 지속되는 구간도 빠짐없이 반영.

## 2. 그래프

- 가로축 = 시간, 세로축 = 값.
- 보기 방식 두 가지: 레인(채널마다 한 줄, 각자 세로 눈금) / 겹쳐보기(세로 눈금 공유)
- 확대/축소. 휠로 확대할 때 한 칸에 너무 많이 커지지 않게.
- 시간축을 옮겼을 때 세로 배율이 갑자기 풀리지 않게
  (기본은 전체 범위 고정, "보이는 구간에 맞춤" 은 버튼으로 켤 때만).
- **값 축(세로) 확대도 가능해야 함.** Shift+휠, 커서가 가리키던 값이 제자리에.
  세로 눈금 모드: 원래값 / 0–1 정규화 / 변화만. 레인에서도 "보이는 구간에 맞춤".
- 세로 눈금 숫자와 실제 그래프 높이가 항상 일치.
- 커서 A/B, 커서 위치의 값 표시.
- 성능: 픽셀 열마다 최소/최대만 뽑아 그릴 것.
- 로그 여는 시간도 짧게 (표를 통째로 복사/전치하지 않기 등).

## 3. IO 선택

- 처음 시작할 때 아무것도 선택 안 된 상태.
- 전체 선택 / 전체 해제 버튼.
- Shift, Ctrl 로 여러 개 선택. **Shift 범위는 화면에 보이는 순서를 따를 것.**
- 다른 IO를 선택했을 때 이전 선택이 꺼지면 안 됨. 같은 IO를 다시 클릭할 때만 꺼짐.
- IO 이름 검색창. 마우스를 그래프 위에서 움직여도 깜빡이면 안 됨.
- 타입 필터(전체/디지털/아날로그/상태).
- **고른 IO만 보는 목록** 필터. 순서는 그래프에 그려지는 차례 그대로.
- 그래프에 그려지는 순서가 왼쪽 목록 순서를 따라갈 것.

## 4. 그룹

- IO를 10개 단위로 묶어서 그룹화, 왼쪽에서 그룹 토글.
- **그룹 이름 변경 가능.**
- **드래그나 검색 추가로 원하는 IO를 그룹에 넣기.**
- **그룹 순서 변경 가능.**
- 그룹 설정은 저장되어 프로그램 껐다 켜도 유지
  (구성원을 채널 번호가 아니라 IO 이름으로 저장).
- 그룹 전체 선택 체크박스, 그룹 삭제, 고른 IO를 그룹에 담기.
  버튼은 기호(+, ×)만 쓰지 말고 무슨 기능인지 글자로.

## 5. 두 로그 비교 (이전 / 이후)

- 같은 양식의 로그 2개로 각 IO의 차이 보기.
- 이전과 이후 그래프의 색이 확실히 구분되게.
- 겹치는 부분 구분: 차이 구간 음영, 벌려 그리기 옵션.
  (점선은 쓰지 말 것 — 직선과 빈 공간 때문에 파형 표현이 나빠짐)
- 두 로그의 시작 시간이 다를 수 있으니 align (자동 + 수동 밀기).
- **IO 존재 차이도 알 수 있게.** 이전에만 있는 IO / 이후에만 있는 IO 를
  각각 모아서 한 화면에서.
- 차이량 측정 방법 여러 개 (최대/평균/RMS/면적/시간비율/샘플수/구간수 등).
  허용 오차 조절 가능.
- 값이 달라진 IO만 보는 필터.

## 6. 설정 창

- 오른쪽 위 설정 버튼.
- 설정 창에서 프로그램 버전 확인.
- **로그 세트 6개.** 한 세트 = 이전 로그 + 이후 로그 한 쌍. 1~6번.
- 세트마다 각각 default 로 열 폴더 설정 (이전용/이후용 따로).
- 설정도 저장되어 프로그램 껐다 켜도 유지.

## 7. UI 일반

- 설명이나 숫자, 버튼들이 서로 겹치면 안 됨. 창이 좁아지면 줄바꿈, 버튼이 영역 밖으로
  튀어나오지 않게.
- 버튼이 무슨 기능인지 알 수 있어야 함 (화살표 같은 기호만 있으면 안 됨).
- 로그 열 때 진행 표시 + 취소 버튼. 읽는 동안 창이 멈추면 안 됨(배경 스레드).
- 다크/라이트 테마, 모니터별 DPI 대응.

## 8. 작업 방식

- 폰으로 프롬프트, git 은 PC 에서 봄. 답변 내용을 git 에서 볼 수 있게
  대화 내용 저장 폴더(`claude-log/`)에 답변도 저장.
- 파일 업데이트할 때마다 버전 0.01씩 올림. 버전은 한 곳에만 두고,
  설정 창에 버전이랑 커밋 해시가 같이 보이게.

## 9. 화면 (이번에 추가된 요구)

- UI 가 깔끔할 것. 기본 바탕은 흰색, 위의 메뉴 목록들은 진한 파랑.
- 바로 그래프가 아니라 **대시보드가 먼저** 나올 것.
  - 이전과 이후 log 분석 결과를 네모칸으로 각각 나눠서:
    IO 개수 (io 변수명 자체가 달라진 것은 괄호치고 몇 개 발생으로) /
    차이 발생 IO 개수 / 큰 네모칸으로 차이가 난 IO 목록
- io 목록에서 io 를 클릭하면 그래프 창으로 넘어감. 위 메뉴에서도 이동 가능.
  그래프 창에서 클릭한 io 만 자동으로 체크되면서 그래프가 보이게.
- 메뉴로 직접 그래프 창에 갔을 때도 이전 프로젝트처럼 왼쪽에 i/o 목록이나 그룹을
  띄우고 체크할 수 있게. 이전 프로젝트 기능도 거의 다 반영.

## 10. 화면 기술 (추가 요청)

> winforms 말고 wpf로해줘

- 화면은 **WPF** 로. 파싱/데이터(`LogScope.Core`)는 그대로 둘 것.

## 11. 히트맵 (추가 요청)

> 위에 메뉴로 이동하는 새 창중에 heatmap이라는 걸 만들어줘
>
> 차이가 나는 io를 왼쪽에 띄워주고 가로축은 시간(1분단위) 해당 시간대에 차이가
> 발생했으면 네모 칸으로 차이 발생 띄워주고 평균 차이값(절대값은 합계로하면 너무
> 커지고 진동이면 +-0이 나올수도 있으니까 다른 좋은 지표로)이 표시된 네모칸를
> 빨간색으로 띄워줘
> 해당 구간에 차이 안발생했으면 네모칸으로 정상 띄워주고 한 0.1% 오차는 오류 아닌거임

- 위 메뉴에서 갈 수 있는 새 화면 "히트맵".
- 왼쪽에 차이가 나는 IO, 가로축은 시간(1분 단위).
- 차이가 발생한 시간대 → 빨간 네모 칸에 그 구간의 평균 차이값.
  - 지표는 합계(구간이 길수록 커짐)도, 부호 있는 합계(진동하면 0)도 아닌 것으로.
- 차이가 없으면 "정상" 네모 칸.
- 값 범위의 **0.1% 정도 오차는 오류로 세지 않을 것.**

## 12. 대화 기록 (추가 요청)

> git에 대화내역 남길 때 내가 보낸 메시지도 남겨

- `claude-log/` 에 답변뿐 아니라 **보낸 요청 원문**도 남길 것.
  → `claude-log/00-user-messages.md`

## 13. 허용 오차 (추가 요청)

> 허용 오차값 디폴트가 0.1%고 사용자가 변경 가능하게 해줘

- 허용 오차 기본값은 **채널 값 범위의 0.1%**.
- 사용자가 바꿀 수 있을 것. 설정에 저장돼 프로그램을 껐다 켜도 유지.
- 대시보드 · 그래프 차이 음영 · 히트맵이 **같은 값**을 쓸 것.

## 14. 그래프 화질과 목록 정렬 (추가 요청)

> 그래프 그려지는 게 좀 이상함 해상도가 낮고 확대하면 거의 사라지네 이부분 고쳐줘
>
> 그리고 차이난 io 정렬할 때 위에서 기준 선택하는게 아니라 목록에서 최대차이 같은
> 지표들 클릭하면 해당 기준으로 정렬되게 정렬된건 삼각형으로 정렬됬다 표시 넣어줘

- 그래프 해상도를 화면 배율에 맞출 것. 확대해도 선이 사라지지 않을 것.
- 차이 난 IO 목록은 위의 콤보가 아니라 **제목 칸을 눌러** 정렬할 것.
- 정렬 중인 칸에 **삼각형** 표시.

## 15. 세로축 단위와 히트맵 값/퍼센트 (추가 요청)

> 아 그리고 히트맵에서 오차 발생 값들 기존 value랑 % 선택해서 볼 수 있게 해줘
>
> 그리고 그래프 창이 좀 분석하기가 힘든 것 같은뎀
>
> 세로축들이 좀 이상하네 일단 세로축에 단위도 적어줘

- 그래프 세로축에 **단위**를 적을 것.
  - 단위는 엑셀의 단위 줄이나 IO 이름 끝의 괄호에서 가져올 것.
  - IO 이름 자체는 엑셀에 적힌 그대로 둘 것 (단위를 떼어내지 말 것).
- 히트맵 칸의 차이 값을 **값 그대로 / 퍼센트** 중에 골라 볼 수 있을 것.
  - 퍼센트의 밑값은 허용 오차와 **같은 값 범위**일 것.

## 16. 허용 오차와 히트맵 칸의 숫자 (버그 지적)

> 이거 히트맵에서 위에 설정하는 허용오차하고 아래 뜨는 오차 퍼센티지가
> 일치하지 않는 것 같은데?

- 히트맵 칸에 적히는 숫자와 위에 설정한 허용 오차가 **같은 잣대**일 것.
  - 빨간 칸의 숫자는 언제나 허용 오차보다 클 것.
- 설명 줄에 기준값도 값과 %로 함께 적을 것.
- 허용 오차를 바꾸고 아직 다시 계산하지 않았으면 그 사실을 알릴 것.

## 17. 세로축 자동 맞춤과 화면 정리 (추가 요청)

> 히트맵에서 구간 클릭했을때 ... 세로축 value가 해당 구간에서 차이 보여줄만큼
> 자동으로 확대나 축소가 안되서 ... 가로축도 잘 맞춰준 것 처럼 세로축도 잘 맞춰줘
>
> 대시보드에서 이전에만 있는 io 이후에 있는 io 칸 크기 줄여주고
>
> e+n으로 10의 n승 표현하면 보기 힘들어 고쳐주고 차이 면적은 필요 없을 것 같아
>
> 그래프에서 전체 구간 보기/ 시간축 확대 시간축소 줄이 안맞는거 맞게끔
> 그리고 알아보기 쉽게 위에 tool바 카테고리별로 구분되게 만들어줘

- 히트맵에서 칸을 눌러 넘어가면 **세로축도** 그 구간에 맞출 것.
- 대시보드의 "한쪽에만 있는 IO" 칸을 작게.
- 숫자를 **지수 표기(e+n)로 적지 말 것.**
- 차이 면적 칸을 뺄 것.
- 도구 줄 버튼들의 줄을 맞추고, **갈래별로 묶어** 보여 줄 것.

## 18. 칸 폭에 따라 IO 순서가 바뀌는 것 (버그 지적)

> 히트맵에서 5분으로 할때랑 1분으로 할 때 io순서가 바뀌던데 뭐임?

- 히트맵 줄 차례는 **칸 폭과 무관**할 것. 같은 로그면 1분이든 5분이든 같은 순서.

## 19. build.bat 한글 깨짐 (버그 지적, 재발)

> 근데 build할때 글자 깨지는건 어케 못함?

- `chcp 65001` 로는 안 됨. 콘솔이 기다리는 인코딩(CP949)으로 파일을 저장할 것.
- 949 가 아닌 콘솔에서는 ASCII 메시지로 물러설 것 — 어떤 경우에도 깨지지 말 것.
- 화면에 찍는 오류 로그도 콘솔 인코딩으로 남길 것.

## 20. 허용 오차의 밑값 (버그 지적)

> 근데 허용 오차를 1%로 했는데 6467과 6466이 차이 난다고 뜨는건 뭘까

- 비율 오차의 밑값은 **값이 오르내린 폭과 값의 크기 중 큰 쪽**일 것.
  - 폭이 좁은 것은 로그가 신호의 한 토막만 담았다는 뜻일 뿐임.
- 실제로 쓰인 **기준값을 화면에서 볼 수 있을 것** (대시보드 목록에 칸).

## 21. 세로 눈금 모드 (질문 + 이름 변경)

> 세로 눈금 원래/ 정규화/변화만 이거 그래프 차이 없는거 맞아?
> 그리고 변화만 대신 변화량으로 해줘 원래값 대신 기존 값으로 해주고

- 레인 보기에서 세 모드가 같은 그림인 것은 맞음 — 그 사실을 화면에서 알릴 것.
- 이름: 원래값 → **기존 값**, 변화만 → **변화량**.

## 22. 컴파일 오류 (제 실수)

> jump라는 local이나 parameter가 선언 안되었다는데

- 같은 메서드에서 바깥/안쪽 범위가 같은 이름을 선언하지 말 것 (C# CS0136).
- 컴파일러 없이도 이 부류를 기계로 잡을 것 → `scripts/check_shadowing.py`.
