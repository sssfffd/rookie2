<Window x:Class="LogScope.App.Views.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="이름" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="ToolWindow"
        Background="{DynamicResource Brush.Window}"
        Foreground="{DynamicResource Brush.Text}"
        FontFamily="Segoe UI" FontSize="12">
    <StackPanel Margin="18,16,18,14">
        <TextBlock x:Name="PromptText" Style="{StaticResource Muted}" />
        <TextBox x:Name="Input" Margin="0,8,0,0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="확인" MinWidth="92" IsDefault="True"
                    Style="{StaticResource PrimaryButton}" Click="OnOk" />
            <Button Content="취소" MinWidth="92" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
